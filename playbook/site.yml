---
- name: Render and validate application XML
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"

  roles:
    - role: ../roles/xml_validation_agent
